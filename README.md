# Port-Scanner
TCP socket: A connection endpoint — ports are "doors" on a host
connect_ex(): Returns 0 if the port accepted the connection (open) 
Threading: Scanning ports in parallel instead of one-by-one (much faster) 
Service detection: Port 80 = HTTP, 443 = HTTPS, 22 = SSH, etc.
